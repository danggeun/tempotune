# Changelog

형식: [Keep a Changelog](https://keepachangelog.com/ko/1.1.0/). 버전은 [SemVer](https://semver.org/lang/ko/).


## [2.0.3] — 2026-09-14

v2.0.2 가 만든 부작용과 그동안 몰랐던 앱 결함을 걷어낸 버전. 새 기능 없음. 네 항목 모두 "규칙이 틀려 있었다" 로 설명된다.
채택 기준(「v2.0.3 계획 (확정안)」 §0): 원인이 재현 스크립트로 확정된 것만, **표준 회귀 세트(합성 7종 + 실녹음) 전 지표 회귀 0** 인 것만.

### 수정 — 트레이스가 연주 중에 끊기던 문제 (T1)
- v2.0.2 의 "유지(held) 프레임은 그리지 않는다" 규칙이 너무 넓었다. 목적은 소리가 끝난 뒤 남는 140 ms 가로줄 제거였는데, **연주 중에도** 활 바꿈·순간 약음으로 한두 프레임씩 유지가 생겨 화면에 뜬 프레임의 **20~31 %** 가 선에서 빠졌다(스타카토·펜타포트 15 는 69 % 만 그려짐). 음이름·바늘은 멀쩡한데 선만 점선이던 원인.
- 이제 `held ≤ 2`(≈46 ms)까지는 잇는다(`core/trace.ts keepInTrace`). 소리가 끝난 뒤 꼬리는 최대 46 ms 로 여전히 짧다. **효과: 69 → 86 %(스타카토) · 69 → 83 %(펜타포트 15) · 78 → 90 %(레전드).** 표시율·라벨 변화·낼 수 없는 음은 전부 불변.

### 수정 — 웹앱이 마이크를 놓지 않아 다른 앱이 녹음을 못 하던 문제 (P1)
- 실측: 웹앱 탭이 열린 채 폰 녹음기로 녹음한 파일이 34.4초 중 **4.18초만 소리**, 그 뒤 **1,452,032 샘플이 정확히 0** — 마이크 스트림이 죽은 것. 안드로이드는 마이크를 한 앱에 주는데, 앱이 숨겨져도 마이크를 쥐고 있었다("숨김 = 오디오 그대로" 는 메트로놈을 위한 규칙이었는데 마이크가 함께 묶여 있었다).
- 화면이 숨겨지면 마이크를 놓고, 돌아오면 다시 연다(같은 세션이라 권한을 다시 묻지 않는다; 못 열면 기존과 같은 '탭하여 시작'). 메트로놈은 오디오 스레드라 계속. 웹에서 **녹음 중이면 놓지 않는다.** 연습 타이머는 숨김으로 멈추지 않는다(연습이 끝난 게 아니다).

### 수정 — 메트로놈 정박마다 튜너에 엉뚱한 음(라6)이 뜨던 문제 (M1)
- 원인(실측, `scripts/metro-blip.mjs`): 클릭 구간을 '예약 시각 + 출력 지연' 으로만 잡아 **마이크 쪽 입력 지연**(안드로이드 20~80 ms+, 블루투스 100 ms+, 표준 API 없음)을 몰랐다. +80 ms 만 어긋나도 클릭이 창 밖으로 새어 **라6(1760 Hz) = 강박 클릭의 음높이**가 떴다 사라졌다. 기각된 가설: 감도 인하 때문(3단계 전부 무관) · 클릭 프레임이 신뢰도 ¼로 샘(문턱상 불가능, 실측 0개).
- 기각한 해법: 여백 확대(80 BPM 에서 박 간격의 32 % 동안 튜너가 멎고 빠른 템포는 전체 정지) · 클릭 주파수 배제(라6 를 진짜 연주하면 안 뜸).
- **자가 보정**(`core/metro/arrival.ts`): 마이크 신호의 블록(≈5 ms) 차분 에너지에서 클릭 어택 스파이크를 예상 시각 근처에서 찾아 오차를 재고, 최근 6개 중 3개가 ±30 ms 에 모이면 그만큼 구간을 민다(80 BPM 에서 3박 ≈ 2.3초 안에 수렴). 못 찾은 박(연주 중·이어폰)은 보정을 밀어내지 않는다. **검증: 오차 +80/+120/+200 ms 에서 수렴 뒤 0 프레임, 추정 ±12 ms · 오차 0 기기는 프레임 열 완전 동일 · 이어폰이면 보정 안 켜짐.** 분석기·감지기 무접촉.
- 실기기 확인: `__gp.stats().metroCalibMs` 가 보정값(ms)을 보여준다.

### 변경 — 다크 고정 뒤 남아 있던 라이트 시절 대비 비율 (D1 · D2 · D3)
- 토큰 전부를 WCAG 대비비로 재고, 후보를 같은 화면에 덧입혀 **그림으로 비교해**(`scripts/render-design.mjs`) 정했다.
- **D1** 보조문 `--text-3` #7c7c7c → **#8a8a8a** (4.2 → 5.3 — 토큰 주석의 "AA 를 넘긴다" 약속 이행) · **D2** 튜너 이명(레♭·C♯·D♭) #555 → **#7a7a7a** (2.8 → 4.9) · **D3** 조작 요소 테두리 `--line-ctl` 신설 **#6a6a6a** (1.4 → 3.2, WCAG 1.4.11). 12개 조작 요소에만; 카드·면 구분선은 "얇고 낮게" 그대로.
- 그림으로 보고 **뺀 것**: 큰 음이름 #fff → #ededed(차이가 안 보이고 순백은 의도된 위계) · 바탕 #121212(메트로놈 카드와의 간격을 잃음).

### 도구
- `scripts/busy-compare.mjs` — **표준 회귀 세트.** 합성 7종(+실녹음)을 돌려 표시율·라벨 변화·스침·낼 수 없는 음·트레이스 그려짐을 내고, `--save/--compare` 로 변경 전후를 셀 단위로 비교해 **나빠진 칸이 하나라도 있으면 실패**한다.
- `scripts/e2e.mjs --only <정규식>` — 관련 시나리오만 골라 돌린다 (「작업 효율 규칙」).
- `scripts/render-design.mjs` — 색·대비 후보를 실제 화면에 덧입혀 나란히 찍는다.
- `scripts/metro-blip.mjs` — M1 재현.

### e2e 수정
- `context suspended externally` 가 v2.0.2 부터 가끔 실패하던 원인은 앱이 아니라 **검사의 경합**이었다 — 앱이 statechange 에서 즉시 되살리는데 그 사이의 'suspended' 를 단언했다. "결국 running 으로 돌아오는가" 만 검사한다.

### 검증
- 단위 **183** (+16) · e2e **48** (+2) · 스크린샷 12장(D 로 재촬영) 0 px · 벤치 41신호 **전 지표 차이 0** · `git diff www/src/core/pitch www/src/core/playing` **비어 있음**(음높이·감지 경로 무접촉) · `busy-compare` 회귀 없음

### 알려진 특성 · 이월
- 클릭 보정이 맞으면 클릭 창이 연주 프레임에 정확히 걸려 박마다 짧은 유지가 생긴다 — 지연 오차가 없던 기기의 v2.0.2 동작과 같다(설계).
- 트레이스에서 **같은 음 안의 한 프레임 40 ¢ 이상 점프**가 실녹음 분당 ~7회 긴 사선으로 보인다. 조사 결과 **그리기 규칙의 문제가 아니라 남은 측정 오차의 그림자**여서 손대지 않았다(T2, 조사 끝): 1~2 프레임짜리 온음 규모 이탈(짧은 음·현 넘김·오추정)을 표시값이 12 %씩 따라가는 자리다. "트레이스는 평활값 말고 측정값을 그려야 한다" 는 안은 **측정으로 기각** — 측정값의 같은 점프는 24·37회로 표시값(7·8회)보다 3~4.6배 많다. 평활이 잡음을 눌러주고 있었다.

## [2.0.2] — 2026-09-13

아이폰 웹앱 실사용 피드백(「실측 발견 — iPhone 웹앱」 B8–B13)에서 나온 결함 수정 + 중음(더블스톱) 연습 표시(B17).
표시 경로만 고쳤다 — **음높이 추정·연주 감지의 출력은 v2.0.1 과 완전히 같다**(벤치 41신호 전 지표 차이 0, 감지기 프레임 단위 동일성 테스트).

### 수정 — 녹음 내보내기 (아이폰에서 파일을 보낼 수 없던 문제)
- **아이폰 녹음이 `.webm` 으로 나와 열 수도 보낼 수도 없던 회귀.** iOS 18.4 부터 Safari 가 `audio/webm;codecs=opus` 녹음을 지원하게 되면서, webm 을 우선하던 목록이 아이폰에서 진짜 WebM 을 만들기 시작했다(코드가 아니라 브라우저가 변해서 생긴 회귀). **iOS 에서만** mp4(AAC) 를 우선하도록 바꿨다 — 비 iOS 는 v2.0.1 과 동일한 webm 이다. Chromium 의 `audio/mp4` 는 실제로 **MP4 안의 Opus** 를 내놓아(실측) 이름만 m4a 인 더 나쁜 파일이 되므로 전 플랫폼 mp4 우선은 채택하지 않았다.
- **확장자를 `mimeType` 문자열이 아니라 파일 내용(`ftyp` / EBML)으로 판정.** mimeType 이 비거나 예상과 달라도 이름과 내용이 항상 일치한다.
- **웹 저장이 iOS 공유 시트를 쓴다.** `<a download>` 만 있던 경로에 `navigator.share({files})` 를 앞세워 파일에 저장·AirDrop·메시지·카톡이 한 번에 열린다(안드로이드 앱의 공유 시트와 같은 경험). 취소는 오류로 처리하지 않는다.
- **옛 webm 녹음 구제** — 아이폰에서 그 파일을 다운로드하면 자동으로 WAV 로 변환해 건넨다(10분 이하).

### 수정 — 튜너 표시
- **정확히 짚은 스케일에서도 음이 바뀔 때마다 바늘·트레이스가 끝까지 튀던 문제(B14).** 적응 부스트는 표시값을 한 프레임에 새 음까지 끌어다 놓는데 음이름 라벨은 확인 프레임을 기다린다 — 그 어긋난 한두 프레임 동안 화면에 **두 음의 간격(실측 최대 190 ¢)** 이 찍혔다. cents 는 정의상 '가장 가까운 음에서 얼마나 벗어났나' 이므로 ±50 ¢ 를 넘을 수 없다 → 값과 라벨이 반음 넘게 벌어지면 라벨을 즉시 맞춘다. 80 BPM 스케일(±5 ¢ 정확도) 실측: 최대 편차 **88 → 47 ¢**, 전환 지점 Δ **92 → 31 ¢**. 비브라토 라벨 안정성은 5.5 Hz ±20~50 ¢ 전 구간에서 **변화 없음**(전환 0회).
- **경계에 걸친 프레임 한 개를 트레이스에서 버린다(B15).** 분석 창이 93 ms 라 음↔음·소리↔무음이 바뀌는 순간의 창 하나는 두 상태에 걸친다 — 실측상 전환마다 정확히 한 프레임만 +10~+21 ¢(음이 꺼질 때는 +47 ¢)로 튀고 앞뒤는 ±2 ¢ 로 안정적이다. 연주가 아니라 창의 부작용이므로 그리지 않는다. 21 ms 를 버리는 대신 앞뒤의 안정값이 곧바로 이어진다.
- **유지(held) 프레임은 트레이스에 쌓지 않는다.** 소리가 끊겼을 때 마지막 값을 최대 140 ms 복사해 내보내던 것이 가로줄로 남았다. 바늘·음이름은 그대로 유지한다(활 바꿈에 깜빡이지 않게).
- **음이 바뀌는 자리를 무조건 끊지 않는다.** 가로축은 '지금 음에서 얼마나 벗어났나' 라 음이 바뀌어도 이어지는 양이다 — 정확히 짚은 스케일이 점선으로 끊기면 "제대로 짚었다" 는 정보가 사라진다. 오차 차이가 40 ¢ 를 넘어 **초록 띠를 가로지르는 가짜 '맞음'** 을 그릴 때만 끊는다 (근거: 정확한 스케일의 전환 Δ 는 평균 22 ¢ · 최대 31 ¢, 흔들린 연주는 85 ¢ 초과).
- **음이 바뀔 때 트레이스에 가로줄을 긋던 문제(C1).** 가로축은 '현재 음 기준 cents' 라 도(−40 ¢) → 레(+45 ¢) 는 기준이 바뀐 것뿐인데 85 ¢ 를 가로지르는 흰 선이 그어졌다. 실측에서 음 유지 길이 중앙값이 128 ms 라 빠른 패시지에서는 초당 7~8개의 가로줄이 쌓여 세로 궤적을 덮었다. 이제 음이 바뀐 자리는 잇지 않는다(갭).
- **게이지 바늘이 음 전환 때 좌우로 쓸고 가던 착시.** 전환 프레임에만 CSS 전이를 끈다. 정보는 그대로, 애니메이션만 사라진다.
- **트레이스 창을 초 단위로 정의하고 7.7초 → 4.0초로 줄였다.** 프레임 개수(360)로 고정돼 있어 **기기 샘플레이트에 따라 창 길이가 달랐다**(48 kHz 7.68초 / 44.1 kHz 8.36초). 이제 어느 기기에서나 4.0초.

### 수정 — 약음기 연습이 안 잡히던 문제 (실사용 최우선 피드백)
- **마이크 감도 3단계를 전부 8~12 dB 내렸다** (.024/.014/.008 → **.010/.005/.002**). 근거: 금속 약음기를 낀 실제 녹음과 대조군을 **같은 크기로 맞춰** 분석기에 넣으면 표시율이 89 % 로 대조군(77 %)보다 오히려 높다 — **약음기 소리 자체는 문제가 없고 그냥 작을 뿐**이다. 그런데 v2.0.1 의 '높음'(−41.9 dBFS)에서는 −40 dBFS 로 줄어든 약음기 소리의 37 % 만 잡혔다. 새 '높음'(−54 dBFS)에서 **84 %**.
- **내리는 비용을 쟀다**: 문턱을 −58 dBFS 까지 내려도 백색잡음·분홍잡음·무음의 오검출은 **전부 0 %**. 잡음을 거르는 것은 크기 문턱이 아니라 그 뒤의 검사(주기성·배음 수·평탄도)이기 때문이다. 실제 방 소음은 확인 필요 — 거슬리면 한 단계 내리면 된다.
- **감도 마이그레이션이 '숫자' 가 아니라 '사용자가 고른 단계' 를 옮기도록** 고쳤다. v1 의 `rms` 와 v2 의 `rmsMin` 은 값 범위가 겹쳐서(`.005` 는 v1 '높음' 이자 v2.0.2 '보통') 저장 키로 표를 구분한다.

### 수정 — 중음 패시지에서 화면이 정신없던 문제
- **애매한 순간에는 음이름을 우기지 않는다 (B16).** 실측(사용자 바이올린 「레전드」 6분 51초, 19,289 프레임): 중음이 빠르게 이어지는 구간에서 음이름이 **초당 9~11회** 바뀌고, 표시 프레임의 **5.1 %**(최악 30초 구간 20.5 %)가 **바이올린이 물리적으로 낼 수 없는 음**이었다. 신뢰도로는 못 거른다 — 그 프레임들의 신뢰도 중앙값이 **0.932** 다.
  거를 수 있는 특징은 따로 있었다: **값이 프레임마다 옥타브씩 튄다.** 실제로 한 음을 켜면 그러지 않고 빠른 스케일도 반음~온음 단위로만 움직인다. 최근 4프레임의 원시 추정이 한 옥타브 넘게 벌어져 있으면 표시용 신뢰도를 0 으로 둔다(감지기는 무영향 — `SPEC_MIN_DB` 와 같은 자리).
  **결과(같은 녹음): 낼 수 없는 음 5.08 → 1.98 % · 라벨 변화 9.5·11.2 → 4.3·4.9 회/초 · 0.1초 미만 스침 227·286 → 70·97.** 표시율 93 → 87 %(그 구간에서 직전 음을 붙잡다가 애매함이 140 ms 넘으면 비운다).
  회귀 없음: 지속 중음 8종 위 성부 100 %·라벨 왕복 0, 스케일 띠 안 97.0 → **97.8 %**, 벤치 41신호 차이 0.

### 추가 — 중음(더블스톱) 연습에서 아래 현이 틀린 것을 알려준다 (B17)
- **문제(실측).** 화면은 중음에서 위 성부(멜로디)를 보여준다(v2.0.1 정책, 그 자체는 옳다). 그런데 **아래 음을 25~30 ¢ 틀리게 짚어도** 화면은 위 음만 보고 `0 ¢ · 초록 · 카드 글로우` 로 **"완벽"** 이라고 말했다. 중음 연습에서 이건 틀린 현을 맞다고 보증하는 것이다.
- **고친 것.** 중음일 때 큰 음이름 아래에 **둘째 성부 한 줄**을 적는다 — `더블스톱 · 라4 −28 ¢` (화면 문구는 교본 표기 '겹음' 대신 연주자들이 실제로 쓰는 말로). 그 음이 허용 범위 안이면 초록, 벗어나면 흰색(트레이스와 같은 색 규칙). 그리고 **카드 전체 초록 글로우는 두 성부가 모두 맞을 때만** 켜진다. 바늘·음이름·cents·트레이스는 v2.0.1 과 똑같이 위 성부를 말한다 — 바뀐 것은 "안 보이던 나머지 절반을 같이 보여준다" 뿐이다.
- **공명을 중음으로 착각하지 않는 근거(실측).** 두 배음렬이 보이는 것만으로는 중음이 아니다 — 한 음만 켜도 옆 개방현이 따라 울린다. 구분되는 값은 **두 기본음의 피크 세기 차이**다: 진짜 중음 0.9~9.0 dB(활 배분이 −10 dB 까지 치우쳐도) / 공명은 1/4 세기 11.2 dB · 1/6 14.7 dB · 1/10 19.1 dB / 단음은 쌍 자체가 검출 안 됨. 문턱 **10 dB** + **116 ms 연속 확인**.
  검증: 단음 롱톤(솔3·레4·라4·미5)·±25 ¢ 비브라토·80 BPM 스케일·흔한 공명 6종에서 오검출 **0 %**, 진짜 중음(5도·장3도·단3도·장6도·4도) 검출 **97 %** 이고 둘째 음의 cents 오차는 **±1 ¢**.
- **남는 한계 (정직하게).** ① 개방현이 **1/2 세기(−6 dB)로 매우 강하게** 울리면 중음으로 뜬다 — 다만 그 현은 실제로 울리고 있고 개방현이라 음정이 맞으므로 초록을 빼앗지 않는다. 실녹음에서는 29초에 4회·150 ms 수준. ② **옥타브·유니슨 중음은 검출하지 않는다** — 배음렬과 구분할 방법이 없다(v2.0.1 과 같은 화면). ③ 한쪽이 −14 dB 이상 묻힌 중음은 놓친다(놓치면 v2.0.1 화면이라 무해).

### 변경 — 다크 고정
- **시스템 테마를 따라가지 않고 항상 어둡게.** 앱의 정체성(세리프 워드마크·검정 튜너 카드·초록)이 전부 어두운 쪽인데 라이트 모드에서만 흰 프레임이 검정 카드를 감싸 따로 놀았고, 실사용이 연습실·밤·거리 두고 보는 화면이다. `:root` 가 곧 다크 값이고 `prefers-color-scheme` 분기는 전부 제거(토큰·툴팁·상태바·파형 틴트).
- **PWA 스플래시·상태바 색도 같이** `#f7f8fb` → `#0f0f0f`. 매니페스트는 배경색을 하나만 받아 다크 폰에서도 흰 화면이 번쩍이던 것.

### 수정 — 볼륨
- **녹음 재생에 보정 게인.** 재생이 `new Audio().play()` 라 1.0 을 넘을 방법이 없었다. 녹음 중 실측한 원시 피크를 저장해 재생 시 −1 dBFS 목표로 되돌린다(최대 +18 dB). 피크 정보가 없는 옛 녹음은 기본 부스트 + 소프트 리미터.
- **메트로놈 클릭 에너지 +1.7~2.9 dB, 고역 비중 +3~8 dB.** 감쇠 시간상수가 7.2~9.6 ms 였던 것을 12.8 ms 로(피크는 그대로), 어택에 1.5 ms 고역 트랜지언트를 더했다 — 폰 스피커는 저역을 거의 못 내고 1~3 kHz 이상에서 효율이 가장 높다. 출력단에 소프트 리미터(무릎 아래 완전 항등)를 두어 겹침에서도 하드 클리핑이 없다. 기본 음량을 0.7 → 1.0 으로(슬라이더의 역할은 줄이는 것). 기본값끼리 비교하면 **+4.8~5.7 dB**.
- **iOS 오디오 세션 힌트** — 마이크가 열려 있으면 `play-and-record`, 없으면 `playback` 으로 선언한다. iOS 는 마이크가 살아 있는 동안 출력을 감쇠하거나 수화기로 돌린다.

### 수정 — 설정
- **v1 사용자의 마이크 감도 설정이 마이그레이션에서 버려지던 문제.** v1 값(.015/.010/.005)이 v2 단계와 0.001 안에서 맞지 않아 기본값으로 리셋됐고, `.015` 만 부동소수 오차로 통과해 **단계에 없는 값**이 들어왔다(설정 화면에서 아무 단계도 선택돼 보이지 않음). 이제 같은 단계 인덱스로 옮긴다.

### 도구
- `scripts/click-level.mjs` — 메트로놈 클릭 피크·RMS·고역 비중을 v2.0.1 과 비교 (앱과 같은 코드로 렌더)
- `scripts/render-trace.mjs` — 실제 캔버스 크기로 트레이스 전/후·창 길이 후보 비교 이미지 생성
- `scripts/sim-scale.mjs` — 스케일(도레미파솔라시도시라솔파미레도)을 합성해 **실제 분석기**에 통과시키고 전환 지점 통계를 낸다
- `scripts/render-dual.mjs` — 중음 표시 전/후를 실제 카드로 렌더 (v2.0.1 화면 = 둘째 성부를 주입하지 않은 같은 소리)

### 검증
- 단위 **167** (+55) · e2e **46** (+8) · 스크린샷 12장 **0 px** · 벤치 41개 신호 **전 지표 차이 0**(처리 시간 열 제외 전 행 동일)
- **`scripts/sim-scale.mjs` 신설** — 스케일을 합성해 실제 분석기에 통과시키고 프레임 단위로 검사한다. e2e 트레이스 픽스처는 추정 합성이 아니라 이 출력이다.
- 표시 경로만 고쳤다는 것의 의미: 분석기·트래커 파일은 B14·B16·B17 에서 바뀌었지만 **바뀐 것은 표시용 신뢰도·표시 일관성·성부 읽기뿐**이고, 음높이 추정과 연주 감지의 출력은 벤치 41신호와 `analyzer.test.ts` 의 감지기 프레임 단위 동일성 테스트로 고정돼 있다.

## [2.0.1] — 2026-09-06

실제 바이올린 연습 녹음(6분 51초)을 분석기에 흘려 찾은 결함 수정. 새 기능 없음, 화면 변화 없음.

### 수정 — 튜너
- **중음(더블스톱)에서 연주하지 않은 낮은 음을 표시하던 문제 — 이제 멜로디(위 성부)를 따라간다.** 두 현을 함께 켜면 YIN 이 두 음의 최대공약수(가상 기본음)를 높은 신뢰도로 내서, 바이올린이 낼 수 없는 음(G1 등)이 신뢰도 0.5 이상 프레임의 **23.7 %** 를 차지했다. 가상 기본음의 정수배 자리를 조사해 실제로 울리는 음으로 되돌리고, YIN 이 아래 음을 직접 잡은 경우(개방현 공명·시차)에도 위쪽 단순 비율 자리를 배음렬로 검증해 위 성부를 찾는다. 합성 중음 전 조건(음량 1~6배 불균형·시차·잡음)에서 위 성부 100 %. 약한 기본음 단음(첼로 C2·베이스 E1)을 중음으로 오인하지 않도록 정합성 거부권.
- 스펙트럼에 에너지가 없는 음은 표시 경로에서 배제 (연주 감지에는 원본 신뢰도 유지 — 중음 구간 연습시간이 빠지지 않게).
- 실측(v2.0.0 → 2.0.1, 같은 녹음): 가짜음 23.96 % → **5.05 %** · 중음 구간 55.69 % → **10.6 %** · 지속 중음 안 라벨 흔들림 2.6 % · 표시율 93.9 % → 92.5 % · 연주 감지 19,292 프레임 중 다른 것 0개.

### 수정 — 벤치마크
- 옥타브 오류를 ±1옥타브만 세던 것을 모든 배음 관계(×k·÷k·×3/2)로 확대. 이전에는 2옥타브 오류가 −2400 ¢ 로 센트 통계에 섞였다 (`violin_A4_snr0` centsBias −1891 ¢ → −0.5 ¢).
- 정상상태 프레임을 맞음/배음오류/누락 세 갈래로 나누고 같은 분모를 쓰도록.
- `scripts/analyze-recording.mjs` — 정답 라벨 없는 실제 녹음 진단 도구 신설.

### 검증
- 단위 112 (+39) · e2e 38 · 스크린샷 12장 0px · 합성 신호 40개 중 39개 무변화(1개 개선) · 합성 중음 12종 12/12 · 중음 8쌍 × 음량·시차 조건 전부 위 성부 100 % · 기본음 −60 dB 단음 유지 · 독립 DSP 리뷰 1회(회귀 1건 지적 → 수정)

## [2.0.0] — 2026-09-05

v1(단일 파일 웹 프로토타입)을 기능·디자인 언어를 유지한 채 전면 재구성. 로드맵 Phase 0–7 ([docs/ROADMAP.md](docs/ROADMAP.md)).

### Phase 0 — 기준선
- 합성 현악기/잡음/말소리 신호 생성기, v1 튜너 벤치마크 수치, 스크린샷 기준선, 설계서·로드맵·기능 체크리스트

### Phase 1 — TypeScript 전환 + 구조 분리
- 단일 파일 → `core / state / audio / persist / platform / ui` 층 (동작 변경 0, 스크린샷 diff 0), 모듈 경계 검사, strict TypeScript
- 수정: 편집기 파일명이 재시작 후 되돌아가던 버그, 편집기 고아 오디오 재생, BPM 로드 클램프, 설정 즉시 저장(pagehide)

### Phase 2 — 오디오 코어
- FFT 기반 YIN(O(N log N)) + 신뢰도, 스펙트럼 옥타브 교정, 신뢰도 가중 트래커(비브라토 적응)
- AudioWorklet 캡처 → Web Worker 분석 파이프라인 (`ScriptProcessorNode`·`AnalyserNode` 제거)
- YAMNet(온라인 모델) 제거 → 신호처리 기반 연주 감지기 (오프라인, 첼로/베이스 저음 40 Hz)
- 워커 ready 대기, 백그라운드 복귀 시 오래된 청크 폐기, 백프레셔, 버퍼 재활용

### Phase 3 — 메트로놈 / 기준음
- 메트로놈을 AudioWorklet 안에서 샘플 정확도로 합성. BPM 변경 즉시 반영, 6/8 중간 액센트
- 단일 AudioContext (마이크 없이 기준음 재생), 유휴 시 컨텍스트 일시정지, `interrupted` 복구, 출력 지연 기반 클릭 뮤트 창

### Phase 4 — 녹음 / 편집기
- IndexedDB v3: 북마크·A-B·파형 영속, meta 스토어 분리. 파형 미니맵, A-B 핸들 드래그·±0.25 s, 삭제 실행 취소, 표시명 `M/D HH:MM`
- Android 네이티브 저장 (Filesystem + Share), 저장 실패 표면화, webm 길이 Infinity 폴백

### Phase 5 — 앱 완결성
- PWA: Service Worker 프리캐시(prompt 모드, 유휴 시 적용), manifest, 아이콘, 자체 호스팅 폰트
- 생명주기 매트릭스, 권한 흐름(미결정/거부/차단), 조용한 실패 전수 제거, 백그라운드 진입 시 녹음 저장, 무활동 감시 독립화, 메트로놈 wake lock, 녹음 60분 상한, Android 뒤로가기, 매니페스트 권한 보정 스크립트

### Phase 6 — UI/UX
- UI/UX 감사(의도 복원·사용 상황 분석) → 디자인 토큰 체계 (경계 1 px, 반경 16/10/7, 조작 48/36 + 히트 44, 액센트 AA)
- 다크 토스트, 상태 점, 반복 프리롤, 속도 프리셋 + 녹음별 기억, 목록 메타, 편집기 진입 페이드, 타이머 초기화 실행 취소, ❚❚
- 독립 리뷰 38건 반영: 선택 상태 통일, 페이지 상단 정렬, 편집기 부제·버튼 정리, 🔈 → 음량, 해요체

### Phase 7 — 최종 검토 + 마감
- **결함 수정**: 기준음 ≠ 440(바로크 415 등)에서 음이름이 반음 틀리던 문제 · 메트로놈+튜너 동시 사용 시 빠른 템포에서 튜너가 얼어붙던 문제 · 녹음 정지 직후 재시작 경합 · openMic 도중 닫힘 · 워클릿 좀비 · `play()` 거부 · 저장 설정 범위 검증
- **제품**: 헤더 'A 듣기', 음이름 도레미/C D E 설정, 편집기 구간 확대, 게이지 ♭♯ 단서, 재생 중 메트로놈 펼치기, 앱 첫 실행 권한 안내·차단 안내, 녹음 보관 30일/계속, 버전 표시, 360 px 헤더, 엣지투엣지 safe-area, 죽은 기준음 바텀시트 제거
- **비주얼 최종 패스**: 타입 스케일 11단계·굵기 3종·4 px 간격 토큰, 폰트 글리프 → SVG(≡ ✕ ← 쉐브론 ♩), 편집기 빨강 12→6, 원거리 위계(띠 .38, 태그라인 이동), 조작 높이 5종, 북마크 대비
- **브랜드**: 로고 공모 — 후보 14안 → 3인 독립 심사 → 하이브리드 6안 재심사 → 띠 형태 6안 → 최종 'Go' + 초록 게이지(허용 띠 + 흰 바늘, 검정 #0d0d0e). 잉크 실측 기반 락업(띠 = 글자 폭 75 %, 간격 5.3 %, 바늘 2.6 % + 돌출), 2패스 광학 정렬, 아이콘 전용 헤어라인 획 보정, adaptive/maskable/테마 아이콘 레이어, 앱 이름 "Go practice"
- **마감**: GitHub Actions CI(check·build·e2e·스크린샷) + Pages 배포, README/ARCHITECTURE/CHANGELOG, MIT 라이선스, Windows 호환 스크립트(cross-env), 매니페스트에 버전·세로 고정 보정, `.gitattributes`

### 검증
- 단위 테스트 73개, e2e 시나리오 38개(가짜 마이크 WAV 주입), 스크린샷 회귀 12장, 튜너 벤치마크 39 신호

## [1.0.0] — 2026-06-01
- 단일 파일 웹 프로토타입 (YIN + FFT 교차검증 튜너, lookahead 메트로놈, YAMNet 연주 감지, IndexedDB 녹음, Capacitor Android)

[2.0.1]: https://github.com/danggeun/go_practice/compare/v2.0.0...v2.0.1
[2.0.0]: https://github.com/danggeun/go_practice/compare/fd56de1...v2.0.0
